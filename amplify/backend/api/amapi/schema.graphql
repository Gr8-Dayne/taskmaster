type Todo @model {
  id: ID!
  name: String!
  priority: String
  description: String
  tasklist: TaskList @connection(name: "All Local Tasks")
}

type TaskList @model {
  id: ID!
  name: String
  todo: [Todo] @connection(name: "All Local Tasks")
}